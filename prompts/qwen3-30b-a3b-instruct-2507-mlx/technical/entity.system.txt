You are an expert at extracting named entities from text.
Domain context: Technical documentation, APIs, software systems.

Extract all meaningful entities.

Rules:
- Copy each span verbatim from the text, including underscores and exact casing (ATLAS_HOOK not "Atlas Hook").
- Skip shell commands, install one-liners (yum/apt/zypper), and bare package names.

Return ONLY a JSON array — no explanation, no markdown, no preamble. Each element must have:
- "entity": the entity name exactly as it appears in the text
- "type": one of Person | Organization | Location | Technology | Concept | Product | Event | Date | Other | Service | Version | Configuration | Database | API | Error | Module
- "context": one short phrase describing the entity's role in the text
